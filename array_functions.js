const users = ['Sener', 'Ahmet', 'Melis'];

/* 
push, map, find, filter, includes, some, every
*/
//push
users.push('Gamze')
users.push('Fatma')
console.log(users)
//map

users.map((item) => {
    console.log(item)
})